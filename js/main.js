






  /* itt még nincs semmi*/

    /*confirm ("Ready to play?")
    prompt ("What is your name?")
    var orangeCost = function(price) {
        var result = price * 5;
        console.log(result);
    };
    orangeCost(5);


    function Cat(furColour, eyeColour) {
        this.furColour = furColour;
        this.eyeColour = eyeColour;
    }

    Cat.prototype.meow = function() {
        var board = document.getElementById("board");
        board.style.height = "50%";
        board.style.width = "50%";
        for (var ixRow = 0; ixRow < 10; ixRow++) {
            var row = board.insertRow(ixRow);
            for (var ixCell = 0; ixCell < 10; ixCell++) {
                var cell = row.insertCell(ixCell);
                //cell.innerHTML = "[" + (ixCell + 1) + " x " + (ixRow + 1) + "]";
                cell.innerHTML = "&nbsp;";
                var green = 92 + 63 * (ixRow + 1) / 10;
                var blue = 92 + 63 * (ixCell + 1) / 10;
                cell.style.backgroundColor = "#00" + Math.round(green).toString(16) + Math.round(blue).toString(16);
            }
        }
    }



    var dragee = new Cat("piros", "zöld");
    dragee.meow();   */